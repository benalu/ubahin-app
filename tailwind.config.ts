// tailwind.config.ts
// This is the Tailwind CSS configuration file for the application


import { type Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      fontFamily: {

      },
    },
  },
};

export default config;
